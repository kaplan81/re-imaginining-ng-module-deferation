@../AGENTS.md

<!--
  Do not delete this file. Claude Code loads CLAUDE.md, never AGENTS.md, so
  without the import above it would start a session with no project
  instructions at all. See https://code.claude.com/docs/en/memory#agents-md
  This comment is stripped before the file enters context, so it costs nothing.
-->

# Claude Code

AGENTS.md above is the single source of truth for this workspace. Put general
instructions there, not here. This file is for Claude-specific instructions only
— things that would make no sense to another agent.
